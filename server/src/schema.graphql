type User {
  id: ID!
  nome: String!
  cpf: String!
  email: String!
}

type Query {
  users(nome: String, cpf: String, email: String): [User]
}

type Mutation {
  createUser(nome: String!, cpf: String!, email: String!): User
  deleteUser(id: ID!): Boolean
}
