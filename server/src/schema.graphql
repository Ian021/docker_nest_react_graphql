type User {
  id: ID!
  nome: String!
  cpf: Int!
  email: String!
}

type Query {
  users(nome: String, cpf: Int, email: String): [User]
}

type Mutation {
  createUser(nome: String!, cpf: Int!, email: String!): User
  deleteUser(id: ID!): Boolean
}
