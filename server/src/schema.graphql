type User {
  id: ID!
  nome: String!
  cpf: Int!
  email: String!
}

type Query {
  getUsers(nome: String, cpf: Int, email: String): [User]
}

type Mutation {
  createUser(nome: String!, cpf: Int!, email: String!): User
  deleteUser(id: ID!): Boolean
}
